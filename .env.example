# SearXNG Configuration
# Generate a secret key using: python -c "import secrets; print(secrets.token_hex(16))"
SEARXNG_SECRET=your-secret-key-here

# Optional: HTTP Proxy Configuration
# Uncomment and configure if you need to use a proxy
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080

# Optional: Basic Authentication for SearXNG
# Uncomment if your SearXNG instance requires authentication
# AUTH_USERNAME=admin
# AUTH_PASSWORD=changeme
